#include "helpers.h"

// Example utility function to process data
int processData(int input) {
    // Perform some processing on the input
    return input * 2; // Example: simply doubling the input
}

// Example utility function to handle input
void handleInput(char input) {
    // Handle the input character
    Serial.print("Input received: ");
    Serial.println(input);
}